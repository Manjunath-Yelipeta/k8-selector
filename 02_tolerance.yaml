apiVersion: v1
kind: Pod
metadata:
  name: nginx-tolerance
  labels:
    env: test
spec:
  containers:
  - name: nginx
    image: nginx
    imagePullPolicy: IfNotPresent
  tolerations:
    - key: "name"
      operator: "Equal"
      value: "sri"